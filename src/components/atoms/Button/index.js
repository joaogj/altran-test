import React from 'react'

import {
  MainContainer,
  ButtonText
} from './styled'

const Button = () => {
  return (
    <MainContainer>
      <ButtonText>Veja Mais</ButtonText>
    </MainContainer>
  )
}

export default Button